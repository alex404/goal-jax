name: Examples

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[examples]"
      # - name: Setup directories
      #   run: |
      #     # Create results directory structure if it doesn't exist
      #     mkdir -p results
      #     chmod 777 results
      - name: Configure matplotlib
        run: |
          echo "backend: Agg" > matplotlibrc
      - name: Run examples
        run: |
          python -m examples.boltzmann.run
          python -m examples.boltzmann.plot
          python -m examples.boltzmann_lgm.run
          python -m examples.boltzmann_lgm.plot
          python -m examples.boltzmann_lgm_cd.run
          python -m examples.boltzmann_lgm_cd.plot
          python -m examples.dimensionality_reduction.run
          python -m examples.dimensionality_reduction.plot
          python -m examples.hmm.run
          python -m examples.hmm.plot
          python -m examples.hmog.run
          python -m examples.hmog.plot
          python -m examples.kalman_filter.run
          python -m examples.kalman_filter.plot
          python -m examples.mfa.run
          python -m examples.mfa.plot
          python -m examples.mixture_of_gaussians.run
          python -m examples.mixture_of_gaussians.plot
          python -m examples.multivariate.run
          python -m examples.multivariate.plot
          python -m examples.poisson_mixture.run
          python -m examples.poisson_mixture.plot
          python -m examples.population_codes.run
          python -m examples.population_codes.plot
          python -m examples.torus_poisson.run
          python -m examples.torus_poisson.plot
          python -m examples.univariate_analytic.run
          python -m examples.univariate_analytic.plot
          python -m examples.univariate_differentiable.run
          python -m examples.univariate_differentiable.plot
      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: examples-results
          path: |
            results/**
